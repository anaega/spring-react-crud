apiVersion: v1
kind: Service
metadata:
    name: my-service
spec:
    selector:
        app: project-app
    ports:
        - protocol: TCP
          port: 8090
          targetPort: 8080
    type: LoadBalancer


